import { css } from "styled-components";

export const botPoseFrontCss = css`
    .pose-front .bot-head {
        top: 0.1rem;
    }

    .pose-front .bot-neck {
        top: 6.75rem;
        left: 50%;
        transform: translateX(-50%);
    }

    .pose-front .bot-torso {
        top: 7.4rem;
        left: 50%;
        transform: translateX(-50%);
    }

    .pose-front .bot-pelvis {
        top: 15.15rem;
        left: 50%;
        transform: translateX(-50%);
    }

    .pose-front .shoulder-left {
        top: 7.95rem;
        left: 1rem;
        width: 1.45rem;
        height: 1.7rem;
        transform: rotate(-18deg);
    }

    .pose-front .shoulder-right {
        top: 7.95rem;
        right: 1rem;
        width: 1.45rem;
        height: 1.7rem;
        transform: rotate(18deg);
    }

    .pose-front .upper-arm-left {
        top: 9rem;
        left: 0.98rem;
        width: 1.2rem;
        height: 3.45rem;
        transform: rotate(8deg);
    }

    .pose-front .upper-arm-right {
        top: 9rem;
        right: 0.98rem;
        width: 1.2rem;
        height: 3.45rem;
        transform: rotate(-8deg);
    }

    .pose-front .forearm-left {
        top: 12rem;
        left: 0.72rem;
        width: 1.2rem;
        height: 3.1rem;
        transform: rotate(10deg);
    }

    .pose-front .forearm-right {
        top: 12rem;
        right: 0.72rem;
        width: 1.2rem;
        height: 3.1rem;
        transform: rotate(-10deg);
    }

    .pose-front .hand-left {
        top: 14.9rem;
        left: 0.66rem;
        width: 0.92rem;
        height: 1rem;
        transform: rotate(8deg);
    }

    .pose-front .hand-right {
        top: 14.9rem;
        right: 0.66rem;
        width: 0.92rem;
        height: 1rem;
        transform: rotate(-8deg);
    }

    .pose-front .thigh-left {
        top: 16.65rem;
        left: 3.75rem;
        width: 1.3rem;
        height: 3.55rem;
        transform: rotate(4deg);
    }

    .pose-front .thigh-right {
        top: 16.65rem;
        right: 3.75rem;
        width: 1.3rem;
        height: 3.55rem;
        transform: rotate(-4deg);
    }

    .pose-front .shin-left {
        top: 19.85rem;
        left: 3.75rem;
        width: 1.3rem;
        height: 3.35rem;
        transform: rotate(4deg);
    }

    .pose-front .shin-right {
        top: 19.85rem;
        right: 3.75rem;
        width: 1.3rem;
        height: 3.35rem;
        transform: rotate(-4deg);
    }

    .pose-front .foot-left {
        top: 23rem;
        left: 2.8rem;
    }

    .pose-front .foot-right {
        top: 23rem;
        right: 2.8rem;
    }
`;